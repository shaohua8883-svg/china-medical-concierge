document.addEventListener('DOMContentLoaded', function() { const navToggle = document.getElementById('navToggle'); const navLinks = document.getElementById('navLinks'); if (navToggle && navLinks) { navToggle.addEventListener('click', function() { this.classList.toggle('active'); navLinks.classList.toggle('active'); }); navLinks.querySelectorAll('a').forEach(link => { link.addEventListener('click', () => { navToggle.classList.remove('active'); navLinks.classList.remove('active'); }); }); } const navbar = document.getElementById('navbar'); let lastScroll = 0; window.addEventListener('scroll', function() { const currentScroll = window.pageYOffset; if (currentScroll > 50) { navbar.style.boxShadow = '0 2px 10px rgba(0,0,0,0.08)'; } else { navbar.style.boxShadow = 'none'; } lastScroll = currentScroll; }); document.querySelectorAll('a[href^="#"]').forEach(anchor => { anchor.addEventListener('click', function(e) { e.preventDefault(); const target = document.querySelector(this.getAttribute('href')); if (target) { target.scrollIntoView({ behavior: 'smooth', block: 'start' }); } }); }); const animateElements = document.querySelectorAll('.section-head, .help-card, .hosp-card, .testimonial-card, .info-card, .faq-item'); const observer = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { entry.target.style.opacity = '1'; entry.target.style.transform = 'translateY(0)'; observer.unobserve(entry.target); } }); }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' }); animateElements.forEach(el => { el.style.opacity = '0'; el.style.transform = 'translateY(30px)'; el.style.transition = 'opacity 0.6s ease, transform 0.6s ease'; observer.observe(el); }); const tlSteps = document.querySelectorAll('.tl-step'); const tlPanels = document.querySelectorAll('.tl-panel'); tlSteps.forEach(step => { step.addEventListener('click', function() { const stepNum = this.dataset.step; tlSteps.forEach(s => s.classList.remove('is-active')); this.classList.add('is-active'); tlPanels.forEach(p => p.classList.remove('is-active')); const panel = document.getElementById('panel-' + stepNum); if (panel) panel.classList.add('is-active'); }); }); const faqItems = document.querySelectorAll('.faq-item'); faqItems.forEach(item => { const question = item.querySelector('.faq-question'); question.addEventListener('click', function() { const isOpen = item.classList.contains('is-open'); faqItems.forEach(i => { i.classList.remove('is-open'); i.querySelector('.faq-question').setAttribute('aria-expanded', 'false'); }); if (!isOpen) { item.classList.add('is-open'); this.setAttribute('aria-expanded', 'true'); } }); }); const inquiryForm = document.getElementById('inquiryForm'); const formSuccess = document.getElementById('formSuccess'); const contactFormDiv = document.getElementById('contactForm'); if (inquiryForm) { inquiryForm.addEventListener('submit', async function(e) { e.preventDefault(); const submitBtn = this.querySelector('button[type="submit"]'); const originalText = submitBtn.innerHTML; submitBtn.innerHTML = 'Sending...'; submitBtn.disabled = true; const formData = new FormData(this); const data = Object.fromEntries(formData); try { const response = await fetch('/api/contact', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) }); const result = await response.json(); if (result.success) { contactFormDiv.style.display = 'none'; formSuccess.classList.add('show'); } else { alert(result.message || 'Something went wrong. Please try again.'); } } catch (err) { console.error('Form error:', err); contactFormDiv.style.display = 'none'; formSuccess.classList.add('show'); } finally { submitBtn.innerHTML = originalText; submitBtn.disabled = false; } }); } const costData = { checkup: { name: 'Full body check-up', sub: 'Executive physical, one day', countries: [{ name: 'United States', price: '$3–8k', wait: 'Multi-visit' }, { name: 'United Kingdom', price: '£2,500', wait: 'Not routine NHS' }, { name: 'Australia', price: 'A$2–5k', wait: 'Multi-visit' }, { name: 'China', price: '$500–1.5k', wait: 'Same day', highlight: true }] }, dental: { name: 'Dental implants & cosmetic', sub: 'Implant + crown, premium brand', countries: [{ name: 'United States', price: '$4–6k', wait: '2–4 weeks' }, { name: 'United Kingdom', price: '£2,500', wait: '3–6 weeks' }, { name: 'Australia', price: 'A$5,500', wait: '2–6 weeks' }, { name: 'China', price: '$900–1.5k', wait: '1 week', highlight: true }] }, onco: { name: 'Cancer treatment / oncology', sub: 'Complex oncology care', countries: [{ name: 'United States', price: '$100–500k', wait: '4–12 weeks' }, { name: 'United Kingdom', price: '£80–250k', wait: '12+ weeks' }, { name: 'Australia', price: 'A$150–400k', wait: '6–14 weeks' }, { name: 'China', price: '$30–120k', wait: '1–2 weeks', highlight: true }] }, cardiac: { name: 'Cardiology & heart surgery', sub: 'Bypass, valve replacement, stents', countries: [{ name: 'United States', price: '$75–200k', wait: '2–6 weeks' }, { name: 'United Kingdom', price: '£35,000', wait: '14+ weeks NHS' }, { name: 'Australia', price: 'A$50–80k', wait: '2–4 months' }, { name: 'China', price: '$20–30k', wait: '1–2 weeks', highlight: true }] }, ivf: { name: 'Fertility / IVF treatments', sub: 'Standard IVF cycle', countries: [{ name: 'United States', price: '$15–25k', wait: '2–4 months' }, { name: 'United Kingdom', price: '£5–8k', wait: 'NHS limited' }, { name: 'Australia', price: 'A$10–15k', wait: '1–3 months' }, { name: 'China', price: '$4–6k', wait: '2–4 weeks', highlight: true }] }, diabetes: { name: 'Diabetes', sub: 'Type 1 / Type 2 program', countries: [{ name: 'United States', price: '$5–15k/yr', wait: 'Ongoing' }, { name: 'United Kingdom', price: 'NHS covered', wait: 'GP-led' }, { name: 'Australia', price: 'A$3–10k/yr', wait: 'GP-led' }, { name: 'China', price: '$1.5–4k/yr', wait: 'In-clinic', highlight: true }] } }; const cmSelect = document.getElementById('cm-select'); const cmCard = document.getElementById('cm-card'); function updateCmCard(key) { const data = costData[key]; if (!data || !cmCard) return; let html = '<div style="font-weight: 600; font-size: 16px; margin-bottom: 4px;">' + data.name + '</div>'; html += '<div style="font-size: 13px; color: #94a3b8; margin-bottom: 16px;">' + data.sub + '</div>'; html += '<div style="display: flex; flex-direction: column; gap: 12px;">'; data.countries.forEach(c => { const bg = c.highlight ? 'background: rgba(37,99,235,0.08); border-color: #2563eb;' : ''; const priceColor = c.highlight ? 'color: #2563eb; font-size: 18px;' : 'text-decoration: line-through; color: #94a3b8;'; html += '<div style="display: flex; justify-content: space-between; align-items: center; padding: 12px; border: 1px solid #e2e8f0; border-radius: 8px; ' + bg + '">'; html += '<div><div style="font-weight: 600; font-size: 14px;">' + c.name + '</div>'; html += '<div style="font-size: 12px; color: #64748b;">' + c.wait + '</div></div>'; html += '<div style="font-weight: 700; ' + priceColor + '">' + c.price + '</div>'; html += '</div>'; }); html += '</div>'; cmCard.innerHTML = html; } if (cmSelect) { cmSelect.addEventListener('change', function() { updateCmCard(this.value); }); updateCmCard('checkup'); } const chatbotToggle = document.getElementById('chatbotToggle'); const chatbotWindow = document.getElementById('chatbotWindow'); const chatbotClose = document.getElementById('chatbotClose'); const chatbotMessages = document.getElementById('chatbotMessages'); const chatInput = document.getElementById('chatInput'); const chatSend = document.getElementById('chatSend'); const chatbotSuggestions = document.getElementById('chatbotSuggestions'); let sessionId = 'session_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9); let isTyping = false; function toggleChat() { chatbotWindow.classList.toggle('is-open'); if (chatbotWindow.classList.contains('is-open')) { chatInput.focus(); } } if (chatbotToggle) chatbotToggle.addEventListener('click', toggleChat); if (chatbotClose) chatbotClose.addEventListener('click', toggleChat); function addMessage(text, sender) { const msg = document.createElement('div'); msg.className = 'chat-message ' + sender; msg.textContent = text; chatbotMessages.appendChild(msg); chatbotMessages.scrollTop = chatbotMessages.scrollHeight; } function showTyping() { const typing = document.createElement('div'); typing.className = 'chat-message-typing'; typing.id = 'typingIndicator'; typing.innerHTML = '<span></span><span></span><span></span>'; chatbotMessages.appendChild(typing); chatbotMessages.scrollTop = chatbotMessages.scrollHeight; } function hideTyping() { const typing = document.getElementById('typingIndicator'); if (typing) typing.remove(); } async function sendMessage(text) { if (isTyping || !text.trim()) return; addMessage(text, 'user'); chatInput.value = ''; isTyping = true; chatSend.disabled = true; showTyping(); try { const response = await fetch('/api/chat', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ message: text, sessionId }) }); const result = await response.json(); hideTyping(); if (result.success) { addMessage(result.reply, 'bot'); } else { addMessage("I'm sorry, I'm having trouble connecting right now. Please contact Warren directly at shaohua8883@gmail.com", 'bot'); } } catch (err) { console.error('Chat error:', err); hideTyping(); const fallbackReply = getFallbackReply(text); setTimeout(() => addMessage(fallbackReply, 'bot'), 500); } finally { isTyping = false; chatSend.disabled = false; chatInput.focus(); } } function getFallbackReply(msg) { const lower = msg.toLowerCase(); if (lower.includes('cost') || lower.includes('price') || lower.includes('how much')) { return "Costs vary by procedure. For example: Full body check-up $500-1.5k, Dental implants $900-1.5k, Cardiac surgery $20-30k. These are typically 60-80% less than US/UK prices. Would you like a free personalized quote?"; } else if (lower.includes('hospital') || lower.includes('doctor')) { return "We partner with 28+ top hospitals in China, including Grade 3A public hospitals and JCI-accredited private facilities. We match you with the right specialist for your condition. What type of treatment are you looking for?"; } else if (lower.includes('process') || lower.includes('how it works') || lower.includes('step')) { return "Our process is simple: 1) Contact us with your medical records → 2) We assess and match you with a specialist → 3) We arrange travel & visa → 4) You arrive, we meet you at the airport → 5) Bilingual care manager accompanies every hospital visit → 6) We handle follow-up and records after you return home."; } else if (lower.includes('contact') || lower.includes('email') || lower.includes('phone') || lower.includes('whatsapp')) { return "You can reach us at: Email: shaohua8883@gmail.com | Contact: Warren. We reply within 24 hours."; } else if (lower.includes('visa') || lower.includes('travel')) { return "We provide full travel support: medical visa invitation letters, flight guidance, hotel booking near the hospital, and airport pickup. Your personal care manager handles everything."; } else if (lower.includes('translate') || lower.includes('language') || lower.includes('english')) { return "Yes, we provide certified medical interpreters who accompany you to every appointment. 100% English support guaranteed. Your care manager is bilingual and available 24/7."; } return "Thank you for your message. To give you the most accurate information, could you tell me more about your condition or what type of treatment you're looking for? Or you can submit a free case review and Warren will personally assess your situation within 24 hours."; } if (chatSend) { chatSend.addEventListener('click', () => sendMessage(chatInput.value)); } if (chatInput) { chatInput.addEventListener('keypress', function(e) { if (e.key === 'Enter') sendMessage(this.value); }); } if (chatbotSuggestions) { chatbotSuggestions.addEventListener('click', function(e) { if (e.target.tagName === 'BUTTON') { const msg = e.target.dataset.msg; if (msg) sendMessage(msg); } }); } const hasVisited = localStorage.getItem('cmc_visited'); if (!hasVisited) { setTimeout(() => { if (!chatbotWindow.classList.contains('is-open')) { chatbotWindow.classList.add('is-open'); addMessage("Hello! I'm Warren. Do you have any questions about medical care in China? I'm here to help!", 'bot'); } }, 30000); localStorage.setItem('cmc_visited', 'true'); } });
